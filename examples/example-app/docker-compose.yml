services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  serverless-redis-http:
    image: hiett/serverless-redis-http:latest
    ports:
      - "8079:80"
    environment:
      SRH_MODE: env
      SRH_TOKEN: example_token
      SRH_CONNECTION_STRING: "redis://redis:6379"
  qstash:
    image: nodejs:24-alpine
    ports:
      - "8081:8081"
    command: npx @upstash/qstash-cli dev -port=8081
